<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.ict.mydream.mypage.MyPageDAO">

<select id="mypage" parameterType="int" resultType="membervo">
select * from tbmember where memno = #{memno}
</select>

<update id="updateProfile" parameterType="membervo">
    UPDATE tbmember
    SET
      nickname = #{nickname},
      categcd = #{categcd},
      password = #{password},
      loccd = #{loccd}
    WHERE memno = #{memno}
  </update>


  <update id="updateImage" parameterType="membervo">
    UPDATE tbmember
    SET
      imgname = #{imgname}
    WHERE memno = #{memno}
  </update>
</mapper>